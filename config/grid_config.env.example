# ==============================================
# 🌐 Grid Configuration for add_grid_labels.py
# ==============================================

# Grid spacing in degrees (e.g. 0.5, 1.0).
# Leave empty to enable automatic spacing selection.
GRID_SPACING_DEG=

# Line-type for grid (dotted, solid or none)
LINE_TYPE = solid

# Color for grid lines (GRID_COLOR), label text (LABEL_COLOR) and background (BACKGROUND_COLOR).
# Accepted formats:
#   ✅ Color name     e.g. "white", "black", "gray", "red", "gold", "none" (for transparent)
#   ✅ Hex color      e.g. "#ffffff", "#00ccff"
#   ✅ Grayscale      e.g. "0.0" (black) to "1.0" (white)
#   ⚠️ RGB tuples like (1.0, 0.5, 0.0) are NOT supported in .env files

BACKGROUND_COLOR = black
GRID_COLOR=white
LABEL_COLOR=white

## Font sizes for Grid label and units
GRID_LABEL_FONT_SIZE = 10
GRID_TICK_FONT_SIZE = 8
